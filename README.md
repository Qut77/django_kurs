# Django_kurs

Учебный проект, созданный в процессе изучения **Django** и сопутствующих инструментов  
(Python, Git, виртуальные окружения, шаблонизаторы).

Проект развивается по мере прохождения курса и постепенно дополняется новым функционалом.

---

## 📌 Описание

На текущий момент проект представляет собой базовое Django-приложение  
с несколькими страницами и первичной настройкой окружения.

Проект используется как:
- практическая песочница для изучения/практического использования Django, Git
- репозиторий для отслеживания прогресса обучения
- основа для дальнейшего расширения (работа с БД, шаблонами, логикой)

---

## 🚀 Текущий функционал

Реализованы следующие страницы:

- `/` — главная страница
- `/about` — страница «О проекте»
- `/contacts` — страница контактов

Также в проекте:
- настроено виртуальное окружение (`venv`)
- зависимости вынесены в `requirements.txt`
- используется шаблонизатор **Jinja** (на базовом уровне)

---

## 🧩 Шаблонизатор

В проекте используется **Jinja** для работы с HTML-шаблонами.  
На текущем этапе — минимальное применение, в дальнейшем планируется:
- работа с динамическими данными
- интеграция с базой данных
- использование шаблонов для отображения моделей

---

## 🛠️ Стек технологий

- Python
- Django
- Jinja
- Git / GitHub
- Virtualenv

---

## ⚙️ Установка и запуск (локально)

```bash
# Клонировать репозиторий
git clone <repository_url>

# Перейти в папку проекта
cd project_name

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows
venv\Scripts\activate
# macOS / Linux
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
python manage.py runserver

# После запуска проект будет доступен по адресу
http://127.0.0.1:8000/